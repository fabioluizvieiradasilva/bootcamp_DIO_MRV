let lines = gets();
console.log(lines);
let N = parseInt(lines.shift());
console.log(N);
return;
//complete o código
let X = gets();

for (  i=1; i<=X; i++  ) {
    if(i % 2 !== 0){
        print(      );
    }
}